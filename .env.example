# Used by the health-check page to call the app's own API route.
# Leave unset locally (falls back to http://localhost:3000) and on Vercel
# (VERCEL_URL is injected automatically per deployment).
NEXT_PUBLIC_SITE_URL=
