export default function Footer() {
  return (
    <footer className="border-t border-border px-6 py-8 text-sm text-ink/60">
      <p>© {new Date().getFullYear()} FlyRankAI — capstone build.</p>
    </footer>
  );
}
